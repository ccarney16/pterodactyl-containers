version: '2.3'
services:
  daemon:
    labels:
      ofelia.enabled: "true"
      ofelia.job-exec.daemon-reload.schedule: "0 30 * * *"
      ofelia.job-exec.daemon-reload.command: "kill -15 1"
  cron:
    depends_on:
    - daemon